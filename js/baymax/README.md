# 相对单位，为了自适应
 不同的移动设备
 750px
 iphone 小手机 XR
 小米，华为
 iphone 大手机 Max
 小米，华为
 vw，vh 按比例分配宽高 %主体容器

 em rem 移动端少用px  细致的每个盒子或盒子模型的大小等比例定义
 em是相对于自身的自体大小来比例
 10em 10*14px=140px
 rem htel 根元素的fontsize
 :before :after 伪元素
 没有标签，但却可以像真正的元素一样来在页面上占位。
 dom 文档流中不需要写这个元素，不会产生副作用不会影响内容显示
 使用css声明，content属性是必须的，
 html 默认的字体大小是16px
 如果没有设置，会使用父级，或body font-size